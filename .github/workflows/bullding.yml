name: Build Flutter Android APK

on:
  push:
    branches: [ "main", "master", "develop" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build_android:
    name: Build Flutter Android APK
    runs-on: ubuntu-latest

    # 🎯 نکته کلیدی: تمام دستورات run در این جاب، درون پوشه agent اجرا می‌شوند
    defaults:
      run:
        working-directory: agent 

    steps:
      # ۱. دریافت کدها (همیشه در ریشه ریپازیتوری انجام می‌شود)
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      # ۲. تنظیم Java
      - name: ☕ Setup Java JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      # ۳. تنظیم فلاتر
      - name: 🐦 Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      # ۴. بررسی نسخه (اجرا در پوشه agent)
      - name: 📋 Show Flutter version
        run: flutter --version

      # ۵. دریافت وابستگی‌ها (اجرا در پوشه agent - خطای قبلی اینجا رفع می‌شود)
      - name: 📦 Get dependencies
        run: flutter pub get

      # ۶. ساخت APK (اجرا در پوشه agent)
      - name: 🏗️ Build Release APK
        run: flutter build apk --release

      # ۷. آپلود فایل خروجی
      - name:
